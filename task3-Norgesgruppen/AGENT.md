# Context: NM i AI 2026 - Task 3 (NorgesGruppen Object Detection)

## 1. Environment & Infrastructure
- GCP Project ID: ai-nm26osl-1730
- Primary Region: europe-north1 (Finland) — for Cloud Run only
- Storage Bucket: gs://ai-nm26osl-1730-nmd-dataset/
- Training VM: yolo-training-vm, zone: europe-west1-b (L4 only available here)
- Working Directory on VM: ~/nm-ai-2026/task3-Norgesgruppen
- Repo: https://github.com/Gjermstad/nm-ai-2026
- Constraints: Scoring sandbox uses Python 3.11, prohibits os module (use pathlib), requires ultralytics==8.1.0 and torch==2.6.0

## 2. Dataset Structure (on Training VM)
task3-Norgesgruppen/
├── preprocess.py
├── train.py
├── run.py (submission entry point)
└── dataset/
    ├── train/
    │   ├── images/         # 248 shelf JPEG images
    │   ├── labels/         # YOLO .txt files (generated by preprocess.py)
    │   ├── annotations.json
    │   └── data.yaml       # YOLOv8 config, path: ".", train: images, val: images, nc: 356
    ├── annotations.json
    └── NM_NGD_product_images/

NOTE: data.yaml is at dataset/train/data.yaml (NOT dataset/data.yaml)
train.py has been patched to use the correct path: data='dataset/train/data.yaml'

## 3. Preprocessing Logic (preprocess.py)
- COCO to YOLO conversion: [x, y, w, h] pixels → normalized [class, x_center, y_center, w, h]
- Direct category mapping: YOLO classes map directly to category_id (0-356)
- Uses pathlib for all file operations (security compliance)
- Generated data.yaml with nc: 356 and full product name list

## 4. Progress Completed
- preprocess.py executed, YOLO labels generated
- Dataset uploaded to GCS bucket
- Training VM created: g2-standard-4, 1x NVIDIA L4, europe-west1-b
- VM image: Deep Learning VM with CUDA 12.4 (pre-installed)
- Boot disk: 100 GB
- GPU confirmed working: nvidia-smi shows NVIDIA L4, CUDA 12.4
- Dataset downloaded from GCS to VM
- ultralytics==8.1.0 installed
- train.py path bug fixed (dataset/train/data.yaml)
- numpy downgraded to 1.26.4 (required for ultralytics 8.1.0 compatibility)
- torch being reinstalled to 2.6.0+cu124 (torch 2.10.0 was incompatible with ultralytics 8.1.0)

## 5. Known Dependency Issues & Fixes
- torch must be 2.6.0 — newer versions break ultralytics 8.1.0 weight loading
- numpy must be <2 (1.26.4) — numpy 2.x breaks pyarrow/pandas on this VM
- After reinstalling torch, delete cached weights: rm -rf ~/.config/Ultralytics/

## 6. Next Steps
1. Confirm torch 2.6.0 install completes successfully
2. Run: nohup python3 train.py > training.log 2>&1 &
3. Monitor: tail -f training.log
4. When done, find best.pt at: runs/detect/yolov8l_norgesgruppen_final/weights/best.pt
5. Package submission.zip with run.py + best.pt
6. Verify run.py uses pathlib (not os), correct category mapping, correct output format
7. Submit at app.ainm.no

## 7. Submission Format Reminder
- run.py must be at zip root
- Output: predictions.json as JSON array with image_id, category_id, bbox [x,y,w,h], score
- Scoring: 70% detection mAP + 30% classification mAP
- Sandbox: Python 3.11, NVIDIA L4, CUDA 12.4, no network, 300s timeout
- Max zip: 420 MB uncompressed