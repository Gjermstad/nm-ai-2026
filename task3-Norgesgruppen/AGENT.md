# Context: NM i AI 2026 - Task 3 (NorgesGruppen Object Detection)

## 1. Environment & Infrastructure
- GCP Project ID: ai-nm26osl-1730
- Primary Region: europe-north1 (Finland) — for Cloud Run only
- Storage Bucket: gs://ai-nm26osl-1730-nmd-dataset/
- Training VM: yolo-training-vm, zone: europe-west1-b
- Working Directory on VM: ~/nm-ai-2026/task3-Norgesgruppen
- Repo: https://github.com/Gjermstad/nm-ai-2026
- Constraints: Scoring sandbox uses Python 3.11, prohibits os module (use pathlib), requires ultralytics==8.1.0 and torch==2.6.0

## 2. Dataset Structure (on Training VM)
task3-Norgesgruppen/
├── preprocess.py
├── train.py
├── run.py (submission entry point)
└── dataset/
    ├── train/
    │   ├── images/         # 248 shelf JPEG images
    │   ├── labels/         # YOLO .txt files (generated by preprocess.py)
    │   ├── annotations.json
    │   └── data.yaml       # Fixed to use absolute paths (see section 5)
    ├── annotations.json
    └── NM_NGD_product_images/

## 3. Preprocessing Logic (preprocess.py)
- COCO to YOLO conversion: [x, y, w, h] pixels → normalized [class, x_center, y_center, w, h]
- Direct category mapping: YOLO classes map directly to category_id (0-355)
- Uses pathlib for all file operations (security compliance)
- Generated data.yaml with nc: 356 and full product name list

## 4. Progress Completed
- preprocess.py executed, YOLO labels generated
- Dataset uploaded to GCS and downloaded to VM
- Training VM created: g2-standard-4, 1x NVIDIA L4, europe-west1-b
- VM image: Deep Learning VM with CUDA 12.4 (pre-installed)
- Boot disk: 100 GB
- GPU confirmed: NVIDIA L4, CUDA 12.4
- ultralytics==8.1.0, torch==2.6.0+cu124, torchvision==0.21.0+cu124 installed
- numpy pinned to 1.26.4 (must stay <2)
- Patched ultralytics tasks.py to use weights_only=False in torch.load
- data.yaml fixed to use absolute paths
- Training started with nohup

## 5. Critical Fixes Applied (do not undo these)
1. train.py: data path changed to data='dataset/train/data.yaml'
2. ultralytics patch:
   sed -i 's/torch.load(file, map_location="cpu")/torch.load(file, map_location="cpu", weights_only=False)/' \
   /opt/conda/lib/python3.10/site-packages/ultralytics/nn/tasks.py
3. data.yaml must use absolute path:
   path: /home/devstar17301/nm-ai-2026/task3-Norgesgruppen/dataset/train
   train: images
   val: images
   nc: 356
   names: (full list from annotations.json)
4. numpy must stay at 1.26.4 — torchvision will try to upgrade it, always pin back with:
   pip install "numpy<2" --force-reinstall --no-deps

## 6. Next Steps
1. Monitor training: tail -f training.log
2. When done, best.pt will be at: runs/detect/yolov8l_norgesgruppen_final/weights/best.pt
3. Verify run.py uses pathlib (not os), correct category mapping, correct output format
4. Package: cd task3-Norgesgruppen && zip -r ../submission.zip run.py best.pt
5. Submit at app.ainm.no (max 6 submissions/day, max 2 in-flight)

## 7. Submission Format Reminder
- run.py must be at zip root
- Output: predictions.json as JSON array with image_id, category_id, bbox [x,y,w,h], score
- Scoring: 70% detection mAP + 30% classification mAP
- Sandbox: Python 3.11, NVIDIA L4, CUDA 12.4, no network, 300s timeout
- Max zip: 420 MB uncompressed
- Blocked imports: os, sys, subprocess, socket — use pathlib instead